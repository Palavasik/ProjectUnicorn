# Инструкция по подключению GitHub и настройке релизов

## 📦 Подключение репозитория к GitHub

### Шаг 1: Создание репозитория на GitHub

1. Перейдите на [GitHub](https://github.com) и войдите в свой аккаунт
2. Нажмите кнопку **"New"** или **"+"** → **"New repository"**
3. Заполните форму:
   - **Repository name**: `ProjectUnicorn` (или другое имя)
   - **Description**: Описание вашего проекта
   - **Visibility**: Public или Private (на ваше усмотрение)
   - **НЕ** ставьте галочки на "Initialize with README", "Add .gitignore", "Choose a license" (мы уже создали эти файлы)
4. Нажмите **"Create repository"**

### Шаг 2: Инициализация Git в локальном проекте

```bash
# Перейдите в директорию проекта
cd /Users/i.s.baranov/Documents/ProjectUnicorn

# Инициализируйте Git репозиторий (если еще не инициализирован)
git init

# Добавьте все файлы в staging
git add .

# Создайте первый коммит
git commit -m "Initial commit: Project structure setup"
```

### Шаг 3: Подключение к удаленному репозиторию

```bash
# Добавьте remote репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/ProjectUnicorn.git

# Или если используете SSH:
git remote add origin git@github.com:YOUR_USERNAME/ProjectUnicorn.git

# Проверьте подключение
git remote -v
```

### Шаг 4: Отправка кода на GitHub

```bash
# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

## 🏷️ Настройка релизов (GitHub Releases)

### Способ 1: Создание релиза через веб-интерфейс GitHub

1. Перейдите на страницу вашего репозитория на GitHub
2. Нажмите на **"Releases"** в правой части страницы (или перейдите по ссылке `https://github.com/YOUR_USERNAME/ProjectUnicorn/releases`)
3. Нажмите **"Create a new release"**
4. Заполните форму:
   - **Choose a tag**: Создайте новый тег (например, `v1.0.0`)
   - **Release title**: Название релиза (например, `Version 1.0.0`)
   - **Description**: Описание изменений (можно использовать шаблон ниже)
   - **Attach binaries**: При необходимости прикрепите файлы
5. Нажмите **"Publish release"**

### Способ 2: Создание релиза через командную строку

```bash
# Создайте и отправьте тег
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin v1.0.0

# Или отправьте все теги сразу
git push origin --tags
```

Затем создайте релиз через веб-интерфейс GitHub, выбрав созданный тег.

### Способ 3: Использование GitHub CLI (gh)

```bash
# Установите GitHub CLI (если еще не установлен)
# macOS: brew install gh
# Linux: см. https://github.com/cli/cli/blob/trunk/docs/install_linux.md

# Авторизуйтесь
gh auth login

# Создайте релиз
gh release create v1.0.0 \
  --title "Version 1.0.0" \
  --notes "Описание изменений" \
  --target main
```

## 📝 Шаблон описания релиза

```markdown
## 🎉 Что нового в версии 1.0.0

### ✨ Новые возможности
- Добавлена команда /start
- Добавлена команда /help
- Базовая обработка сообщений

### 🐛 Исправления
- Исправлена ошибка при обработке пустых сообщений

### 🔧 Технические изменения
- Обновлены зависимости
- Улучшена обработка ошибок

### 📚 Документация
- Добавлена документация по архитектуре
- Обновлен README

---

**Полный список изменений**: [Сравнить с предыдущей версией](https://github.com/YOUR_USERNAME/ProjectUnicorn/compare/v0.9.0...v1.0.0)
```

## 🤖 Автоматизация релизов с GitHub Actions

Создайте файл `.github/workflows/release.yml` для автоматизации:

```yaml
name: Release

on:
  push:
    tags:
      - 'v*'  # Триггер при создании тега, начинающегося с 'v'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Create Release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          body: |
            Автоматический релиз версии ${{ github.ref }}
          draft: false
          prerelease: false
```

## 📋 Рекомендации по версионированию (Semantic Versioning)

Используйте формат `MAJOR.MINOR.PATCH`:

- **MAJOR** (1.0.0) - Несовместимые изменения API
- **MINOR** (0.1.0) - Новая функциональность с обратной совместимостью
- **PATCH** (0.0.1) - Исправления ошибок с обратной совместимостью

Примеры:
- `v1.0.0` - Первый стабильный релиз
- `v1.1.0` - Добавлена новая функция
- `v1.1.1` - Исправлена ошибка
- `v2.0.0` - Крупные изменения, нарушающие совместимость

## 🔐 Настройка SSH ключей (опционально)

Если хотите использовать SSH вместо HTTPS:

```bash
# Проверьте наличие SSH ключей
ls -al ~/.ssh

# Если ключей нет, создайте новый
ssh-keygen -t ed25519 -C "your_email@example.com"

# Скопируйте публичный ключ
cat ~/.ssh/id_ed25519.pub

# Добавьте ключ на GitHub:
# Settings → SSH and GPG keys → New SSH key
```

## 📌 Полезные команды Git

```bash
# Проверка статуса
git status

# Просмотр истории коммитов
git log --oneline

# Просмотр всех тегов
git tag

# Удаление тега (локально и удаленно)
git tag -d v1.0.0
git push origin --delete v1.0.0

# Обновление из удаленного репозитория
git pull origin main

# Создание новой ветки для разработки
git checkout -b develop
git push -u origin develop
```

## 🎯 Чеклист перед релизом

- [ ] Все изменения закоммичены
- [ ] Тесты проходят успешно
- [ ] Обновлен CHANGELOG.md (если используется)
- [ ] Обновлена версия в коде (если используется)
- [ ] README.md актуален
- [ ] Создан тег с правильной версией
- [ ] Описание релиза подготовлено
- [ ] Релиз опубликован на GitHub

## 📚 Дополнительные ресурсы

- [GitHub Docs: Creating Releases](https://docs.github.com/en/repositories/releasing-projects-on-github/managing-releases-in-a-repository)
- [Semantic Versioning](https://semver.org/)
- [GitHub CLI Documentation](https://cli.github.com/manual/)
